public class Node {
    public char value;
    public Node next;
    public Node prevNode;

    public Node(char value, Node next, Node prevNode){
      this.value = value;
      this.next = next;
      this.prevNode = prevNode;
    }

    
}